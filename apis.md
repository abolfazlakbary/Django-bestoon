/submit/expense/
    POST, returns a json
    input: date(optional), text, amount, user
    outpot = status:ok

/submit/income/
    POST, returns a json
    input: date(optional), text, amount, user
    outpot = status:ok

/auth/register/
    POST, registeration
    output: key(token)

/auth/login/
    POST, login
    output: key(Token)

